# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
.env
.venv
venv/
.envrc

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data — raw/processed excluded; simulation results tracked
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep
# data/simulation_results.json  # tracked — remove this line to exclude

# Models tracked via Git LFS (see .gitattributes)
# *.pkl files are committed — large ones use LFS

# Temp / patch scripts
patch*.py
tree_project.txt

# VS Code
.vscode/

# If you want to keep placeholder files in data folders, just add empty .gitkeep files.
.gitkeep