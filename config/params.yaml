model:
  type: "xgboost"
  n_estimators: 300
  max_depth: 3
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
  random_state: 42
  eval_metric: "logloss"

train:
  test_size: 0.2
  stratify: true
  random_state: 42
